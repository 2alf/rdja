use rust_dk::rust;

rust! {
    фк главно() {
        нека x = 5;
        пишиЛинију!("Ћирилица ради: {}", x);

        нека резултат: Резултат<i32, Низ> = Ок(42);
        упореди резултат {
            Ок(бр) => пишиЛинију!("Број: {}", бр),
            Упс(_) => пишиЛинију!("Грешка"),
        }
    }
}
