use rust_dk::rust;

rust! {
    фк главно() {
        neka x = 5;
        пишиЛинију!("MEŠAJJJ БАТОО: {}", x);

        нека резултат: Резултат<i32, Низ> = Ок(42);
        упореди резултат {
            Ок(бр) => pišiLiniju!("Broj je: {}", бр),
            Упс(_) => пишиЛинију!("Грешка"),
        }
    }
}
